'use strict';

/**
 * properties-product service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::properties-product.properties-product');
